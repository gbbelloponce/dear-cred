model Client {
  id        String   @id @default(cuid())
  firstName String
  lastName  String
  phone     String
  address   String
  dni       String   @unique
  notes     String?
  createdAt DateTime @default(now())

  loans     Loan[]

  @@map("clients")
}
