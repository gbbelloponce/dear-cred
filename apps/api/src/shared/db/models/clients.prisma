model Client {
  id        String   @id @default(cuid())
  userId    String
  firstName String
  lastName  String
  phone     String
  address   String
  dni       String
  notes     String?
  createdAt DateTime @default(now())
  deletedAt DateTime?

  loans Loan[]

  @@unique([dni, userId])
  @@map("clients")
}
